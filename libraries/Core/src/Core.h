#pragma once
// Empty on purpose - this "library" only holds examples for the core's
// built-in Arduino API (GPIO, Serial, tone(), malloc(), FLASH_DATA), all
// of which are already available without including anything.
